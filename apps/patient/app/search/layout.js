export const metadata = {
  title: "First Aid Guidance",
  description:
    "Personalized first-aid steps for your symptoms. Call local emergency services if someone is in danger.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SearchLayout({ children }) {
  return children;
}
