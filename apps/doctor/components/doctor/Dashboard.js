"use client";
import { useEffect,useState } from "react";
import { CalendarDays,ClipboardList,FlaskConical,Users } from "lucide-react";
import { errorMessage, readApiResponse } from "@curais/ui";
const cards=[['sharedPatients','Consented patients',Users,'/patients'],['todayAppointments','Today’s appointments',CalendarDays,'/appointments'],['draftEncounters','Draft encounters',ClipboardList,'/encounters'],['outstandingLabOrders','Outstanding labs',FlaskConical,'/labs']];
export function Dashboard(){const[data,setData]=useState(null);const[error,setError]=useState('');useEffect(()=>{fetch('/api/curais/emr/dashboard',{cache:'no-store'}).then(readApiResponse).then(setData).catch(e=>setError(errorMessage(e,"Unable to load the dashboard.")))},[]);return <div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([key,label,Icon,href])=><a key={key} href={href} className="glass-strong rounded-[1.5rem] p-5 transition hover:-translate-y-1"><Icon className="h-5 w-5 text-aid-teal"/><p className="mt-6 font-quicksand text-3xl font-bold">{data?data[key]:'—'}</p><p className="mt-1 text-sm font-semibold text-aid-muted">{label}</p></a>)}</div>{error?<p role="alert" className="mt-4 text-sm text-red-700">{error}</p>:null}<div className="glass mt-6 rounded-2xl p-5"><h2 className="font-quicksand text-xl font-bold">Clinic safety boundary</h2><p className="mt-2 text-sm leading-relaxed text-aid-muted">Clinical actions are available only for patients with an active grant to your clinic. Revocation blocks future access immediately; signed clinical records remain attributable and auditable.</p></div></div>}
